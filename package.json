{
  "name": "expence-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Splitwise-inspired expense sharing app (MERN stack)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}